# Git LFS for large assets (optional)
# To use: install git-lfs, then uncomment these lines
# Then run: git lfs track "*.png" "*.jpg" "*.mp3" "*.wav"

# Image assets
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.jpeg filter=lfs diff=lfs merge=lfs -text

# Audio assets  
# *.mp3 filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text
# *.ogg filter=lfs diff=lfs merge=lfs -text

# Sprite sheets (likely to be large)
# *_spritesheet.png filter=lfs diff=lfs merge=lfs -text
# *_atlas.png filter=lfs diff=lfs merge=lfs -text
