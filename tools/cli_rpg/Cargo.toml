[package]
name = "cli_rpg"
version = "0.1.0"
edition = "2021"
description = "Standalone CLI runner for DJ Engine story graphs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dj_engine = { path = "../../engine" }
rand = "0.8"
